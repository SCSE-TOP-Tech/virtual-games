const loginAPI = async (req, res) => {
  console.log("Backend Logic here");
  //to handle backend logic
  //await ...

  //return ok for now
  res.status(200).json({ status: "OK" });
};

export default loginAPI;
